[package]
name = "reamioui"
version = "0.0.0"
edition = "2024"

[dependencies]
rfd = "0.15" 
serde = { version = "1.0", features = ["derive"] }
slint = "1.11"
ureq = { version = "3.0", features = ["brotli", "json"] }

[build-dependencies]
slint-build = "1.11"
